# WarPy40K Syntax Highlighting Test
# This file tests syntax highlighting in both VS Code and Sublime Text

# Variable declarations
i: dg = 0
name: servitor = "imperial_servant"
power: psykers = 100
shield: void_shields = true

# Assignments
counter = 42
i = i + 1
result = counter + power

# For loops
for i in 1..5:
    the_emperor_protects()
    burn_the_heretic("heretic_" + str(i))

# While loops
while counter > 0:
    for_the_emperor()
    counter = counter - 1

# Conditionals
if power > 50:
    the_emperors_will_be_done()
    ave_imperator()
else:
    fear_is_the_mind_killer()
    burn_the_heretic("weak_psyker")

# Nested loops
for x in 1..3:
    for y in 1..2:
        we_are_angels_of_death()
        purge_the_xenos("nested_xenos")

# Complex expressions
if i == 0 and power >= 100:
    blood_for_the_blood_god()
elif i != 0 or power < 50:
    let_the_galaxy_burn()
else:
    the_path_is_set()

# More commands
WAAAGH()
taste_chaos()
more_dakka()
ork_cunning()
farseers_vision()

# Final commands
we_are_one()
faith_is_my_shield()
pain_is_temporary_glory_is_forever()
only_in_death_does_duty_end()
even_in_death_i_still_serve()
no_pity_no_remorse_no_fear()
we_are_angels_of_death()

# Test all data types
dg_var: dg = 0
servitor_var: servitor = "servant"
blob_var: blob = "data"
psykers_var: psykers = 50
void_shields_var: void_shields = false 